<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.listviewpersonalizado">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroCarros" />
        <activity android:name=".ListarCarros" />
        <activity android:name=".Reportes" />
        <activity
            android:name=".NoDeCarros"
            android:label="@string/title_activity_no_de_carros"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Reporte1" />
        <activity android:name=".Reporte2" />
        <activity android:name=".Reporte3"></activity>
    </application>

</manifest>